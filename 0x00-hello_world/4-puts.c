#include <stdio.h>

/**
 * main: The main function indicates the entry point of the program
 *
 * Return: The return keyword should return 0 in this case
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
